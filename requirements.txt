# Discord
discord.py>=2.4.0

# LLM providers
google-genai>=1.0.0
groq>=0.11.0

# Database
aiosqlite>=0.20.0

# Config
python-dotenv>=1.0.0
pydantic-settings>=2.0.0

# HTTP client
aiohttp>=3.9.0

# Web search
ddgs>=1.0.0
tavily-python>=0.3.0

# Embeddings (local, unlimited, ~130MB model downloaded on first use)
fastembed>=0.3.0
numpy>=1.26.0

# File processing
pypdf>=4.0.0
pdf2image>=1.17.0   # scanned PDF OCR — requires poppler-utils system package
python-docx>=1.1.0
chardet>=5.0.0
python-magic>=0.4.27

# Timezone data (required on Windows; no-op on Linux which has system tzdata)
tzdata>=2024.1

# Audio transcription (via Groq Whisper)
# Already included via groq package
